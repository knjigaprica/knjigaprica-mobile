import 'package:flutter/material.dart';

class LibraryTemplate extends StatelessWidget {
  const LibraryTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Moje knjige'),
    );
  }
}
